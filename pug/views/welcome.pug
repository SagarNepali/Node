doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=s, initial-scale=1.0")
        title Document
    body 
        div
            ul
                - const arr= ["A","b","c"]
                h1 Using array defn in HTML
                for name in arr
                    li.card#name= name
            ul
                h1 Using locals object
                    for name in {names}
                        li= name